# JavaScript의 Hoisting과 Closure 개념 정리

## 1. Hoisting(호이스팅)

Hoisting은 변수와 함수의 선언이 코드의 최상단으로 끌어올려진 것처럼 동작하는 자바스크립트의 특징이다. 실제 코드가 이동하는 것은 아니며, 자바스크립트 엔진이 코드를 실행하기 전에 선언을 먼저 처리하기 때문에 발생하는 현상이다.

`var`는 선언과 동시에 `undefined`로 초기화되지만, `let`과 `const`는 초기화 이전까지 접근할 수 없어 오류가 발생한다.

## 2. Closure(클로저)

Closure는 함수가 선언될 당시의 외부 스코프를 기억하여, 함수 실행이 끝난 이후에도 외부 변수에 계속 접근할 수 있는 특징이다. 이를 활용하면 데이터 은닉과 상태 유지가 가능하며, 다양한 기능 구현에 사용된다.

## 3. Hoisting과 Closure의 차이점

* Hoisting은 변수와 함수의 선언이 먼저 처리되는 실행 방식이다.
* Closure는 함수가 외부 스코프의 변수를 계속 사용할 수 있는 특징이다.
* Hoisting은 코드 실행 전에 발생하는 과정이며, Closure는 함수 실행과 스코프를 기반으로 동작한다.
* Hoisting은 선언 방식에 영향을 받고, Closure는 데이터 은닉과 상태 유지에 활용된다.
