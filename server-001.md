# WS와 WAS의 차이점

## 1. WS(Web Server)

WS는 클라이언트의 HTTP 요청을 받아 **정적 콘텐츠(HTML, CSS, 이미지 등)**를 제공하는 서버이다. 대표적으로 Apache, Nginx 등이 있으며, 주로 정적인 파일을 빠르게 처리하는 역할을 한다.

## 2. WAS(Web Application Server)

WAS는 클라이언트의 요청을 받아 **동적인 콘텐츠와 애플리케이션 로직을 처리**하는 서버이다. 데이터베이스 연동이나 비즈니스 로직을 수행할 수 있으며, 대표적으로 Tomcat, JBoss 등이 있다.

## 3. WS와 WAS의 차이점

- WS는 주로 정적 콘텐츠를 제공하고, WAS는 동적인 콘텐츠와 애플리케이션 로직을 처리한다.
- WS는 정적 파일 처리에 특화되어 있고, WAS는 프로그램 실행과 데이터베이스 연동이 가능하다.
- WS와 WAS를 함께 사용하면 정적 요청과 동적 요청을 분리하여 서버의 효율성을 높일 수 있다.


# Spring의 주요 특징

## 1. DI(Dependency Injection)

DI는 객체가 필요한 의존성을 직접 생성하지 않고 **외부에서 주입받는 방식**이다. 객체 간의 결합도를 낮추고 코드의 재사용성과 유지보수성을 높일 수 있다.

## 2. IoC(Inversion of Control)

IoC는 객체의 생성과 관리 등의 **제어권을 개발자가 아닌 Spring이 담당하는 것**이다. Spring Container가 객체를 생성하고 관리하여 개발자가 객체 관리에 대한 부담을 줄일 수 있다.

## 3. POJO(Plain Old Java Object)

POJO는 특정 프레임워크나 기술에 종속되지 않은 **일반적인 Java 객체**를 의미한다. 특정 기술에 대한 의존성을 줄여 코드의 재사용성과 유지보수성을 높일 수 있다.

## 4. AOP(Aspect-Oriented Programming)

AOP는 여러 클래스에서 공통적으로 사용되는 기능을 **관점(Aspect)으로 분리하여 관리**하는 프로그래밍 방식이다. 로깅, 트랜잭션, 보안 등의 공통 기능을 분리하여 코드의 중복을 줄일 수 있다.

## 5. Spring 주요 특징 정리

- **DI**: 외부에서 객체의 의존성을 주입한다.
- **IoC**: 객체의 생성과 관리를 Spring이 담당한다.
- **POJO**: 특정 기술에 종속되지 않은 일반적인 Java 객체를 사용한다.
- **AOP**: 공통 기능을 분리하여 코드의 중복을 줄인다.
